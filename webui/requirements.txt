# Gatecrash web UI Python dependencies.
#
# Pinned with ~= (compatible-release): allows patch + minor updates so security
# fixes still flow, but blocks the breaking major bump (e.g. Flask-Limiter 3→4).
# Only the four packages Gatecrash imports directly are listed — the rest
# (Jinja2, click, limits, ...) are transitive and resolve within these bounds.
#
# To move to a new major version: test it on the dev box, then bump the pin.
# Baseline known-good versions captured 2026-05-16.

Flask~=3.1          # 3.1.3
Werkzeug~=3.1       # 3.1.8 — Flask's core dependency; the usual breakage source
Flask-Limiter~=4.1  # 4.1.1
bcrypt~=5.0         # 5.0.0
